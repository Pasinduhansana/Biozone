import React from "react";

const Navigation = () => {
	return (
		<div className="absolute top-0 min-h-screen flex items-center justify-center">
			<div className="relative flex flex-row items-center justify-center"></div>
		</div>
	);
};

export default Navigation;
